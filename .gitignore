# Local development only
firebase-setup/serviceAccountKey.json
firebase-setup/node_modules/
firebase-setup/.env
firebase-setup/package-lock.json

# Production Firebase config (Public keys only)
# js/firebase-config.js is COMMITTED because it is required by the frontend at runtime.

node_modules/
.DS_Store
Thumbs.db
